// string_interner.cpp
// StringInterner is header-only, but we provide this compilation unit
// to ensure the header compiles cleanly and to satisfy the CMake target.

#include "common/string_interner.hpp"
